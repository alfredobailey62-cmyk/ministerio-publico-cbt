name: ministerio_publico_cbt
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.2


environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0
  image_picker: ^1.2.1
  google_maps_flutter: ^2.17.0
  geolocator: ^14.0.2
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  firebase_messaging: ^16.2.0
  provider: ^6.1.1
  shared_preferences: ^2.5.5
  # ── PDF y apertura de archivos ──────────────
  pdf: ^3.10.8
  open_file: ^3.3.2
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets\logo.png